import React from "react";
import PageHeader from "antd/lib/page-header";

const Header = () => (
  <PageHeader title="Writer" subTitle="Just keep writing, with a little help" />
);

export { Header };
